---
alwaysApply: true
description: 项目开发原则，以保证项目结构的一致性和可维护性。
---

# 项目开发原则

## 项目结构原则

严格按照以下的目录结构的约定进行项目开发，以保证项目结构的一致性和可维护性：

```txt
src/
├── app.tsx             # 运行时配置、getInitialState、layout
├── access.ts           # 权限定义
├── constants/          # 全局常量
├── utils/              # 工具函数
├── services/           # 按业务拆分的 API（如 services/demo/）
├── models/             # 全局 model（如 global.ts）
├── components/         # 全局/通用组件
├── pages/              # 页面，与路由一一对应
│   └── page-name/
│       ├── index.tsx
│       ├── index.less   # 可选
│       └── components/  # 页面内组件，可选
├── typings.d.ts        # 全局类型声明
│
├── mock/               # 根目录 mock，与 src 平级
└── config/             # 根目录 config，与 src 平级
    ├── config.ts       # 工程配置
    ├── routes.ts       # 路由配置
    └── proxy.ts        # 代理配置
```

## 配置文档优先原则

在项目开发的过程中，可能会涉及到 umi 配置的修改，请遵循以下的原则：

1. 原则上不允许对现有的工程目录结构进行修改；
2. 优先搜索 `https://umijs.org/llms.txt` 文档，查找相关的配置说明；
3. 当文档中的目录结构与现有的工程目录结构不一致时，寻找配置文件相同的部分，进行对应修改。

## 依赖兼容性原则

在项目开发的过程中，可能会涉及到依赖的更新，请遵循以下的原则：

### 包管理器

执行 node 包管理器指令时，请根据下面的优先级判断用户的 `node` 包管理器，并在执行后续操作时使用对应的包管理器命令：

1. 通过读取 `package.json` 文件中的 `packageManager` 字段，判断用户的 `node` 包管理器；
2. 通过读取 `.umirc.ts` 文件或者 `config/config.ts` 文件中的 `npmClient` 字段，判断用户的 `node` 包管理器；
3. 通过依赖的 lock 文件的类型，判断用户的 `node` 包管理器；
4. 如果以上方式都无法判断，则默认使用 `npm` 命令。

### 依赖版本

1. 优先使用项目已经存在的依赖版本，避免重复安装；
2. 当项目中没有适用的依赖版本时，优先通过包管理器安装最新版本的依赖；
3. 当最新的依赖与项目环境不兼容时，优先通过包管理器安装兼容的最新版本依赖。

## 可用组件优先原则

优先使用现有的可用组件，避免重复造轮子：

1. 当项目依赖中有适用的 `pro-components` 组件时，优先使用 `pro-components` 组件，参考 `https://pro-components.antdigital.dev/sitemap.xml`；
2. 当项目依赖中有适用的 `antd` 组件时，优先使用 `antd` 组件，参考 `https://ant.design/llms.txt` 文档，查找相关的组件说明；
3. 当项目有符合需求的已封装 `components` 组件时，优先使用已封装组件；
4. 当项目中没有适用的组件时，再进行兜底实现。

## 可用方法优先原则

优先使用现有的可用方法，避免重复造轮子：

1. 页面处理接口请求时，优先使用 `useRequest` 方法，参考 `https://ahooks.js.org/hooks/use-request/basic` 文档，查找相关的使用说明；
2. 页面需求可以使用 hooks 时，先查找 `ahooks` 文档，查找相关的使用说明；
3. 当项目中有可用的通用方法时，优先封装使用通用方法；
4. 全局可以通用的方法，封装到 `utils` 目录下，并在 `utils` 目录下创建 `index.ts` 文件，用于导出所有通用方法；
5. 当项目中没有适用的方法并全局无法通用时，再在页面级进行兜底实现。

## TypeScript 类型声明原则

原则上不允许声明 `any` 类型，如果需要声明 `any` 类型，请先思考是否可以避免使用 `any` 类型。
